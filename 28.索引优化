两表连接：一个表中有索引，要把有索引的表当做副表
索引失效：
    1.全值匹配
    2.不在索引列上做任何操作（计算，函数、（自动or手动）类型转换），会导致索引失效
    3.存储引擎不能使用索引中范围条件右边的列
    4.尽量使用覆盖索引（只访问索引的查询（索引列和查询列一致）），减少SELECT *
    5.mysql在使用不等于的时候无法使用索引会导致全表扫描
    6.IS NULL,IS NOT NULL无法使用索引
    7.like以通配符开头（'%abc..'）mysql索引失效会变成全表扫描
    8.字符串不加单引号索引会失效
    9.少用or，用他来连接时会索引失效
